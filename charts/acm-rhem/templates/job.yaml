---
apiVersion: batch/v1
kind: Job
metadata:
  name: enable-flightctl-console-plugin
  namespace: student-services
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  template:
    spec:
      serviceAccountName: console-patch-sa
      restartPolicy: Never
      containers:
        - name: patch-console
          image: registry.redhat.io/openshift4/ose-cli:latest
          command:
            - /bin/sh
            - -c
            - |
              oc patch console cluster --type='merge' -p '{"spec":{"plugins":["flightctl-plugin"]}}'